# ========================================
# Sentry Configuration
# ========================================
#
# Copy this file to your .env and fill in the values
# Get your DSN from: https://sentry.io/settings/deepref/projects/
#

# ----------------------------------------
# Backend Configuration (apps/api/.env)
# ----------------------------------------

# Required: Sentry DSN for backend project
# Format: https://<key>@sentry.io/<project-id>
SENTRY_DSN=

# Environment name (development, staging, production)
SENTRY_ENVIRONMENT=development

# Release version (auto-set by CI/CD, optional locally)
# SENTRY_RELEASE=1.0.0

# Performance Monitoring
# Percentage of transactions to sample (0.0 to 1.0)
# Development: 1.0 (100%), Production: 0.2 (20%)
SENTRY_TRACES_SAMPLE_RATE=1.0

# Profiling
# Percentage of transactions to profile (0.0 to 1.0)
# Development: 0.1 (10%), Production: 0.05 (5%)
SENTRY_PROFILES_SAMPLE_RATE=0.1

# Enable/disable profiling
SENTRY_ENABLE_PROFILING=true


# ----------------------------------------
# Frontend Configuration (apps/web/)
# ----------------------------------------
#
# Note: For frontend, configure in:
# - src/environments/environment.ts (development)
# - src/environments/environment.prod.ts (production)
#
# Or set at build time:
# window.SENTRY_DSN = 'your-dsn-here'


# ----------------------------------------
# CI/CD Configuration (GitHub Secrets)
# ----------------------------------------
#
# Set these in GitHub repository settings:
# Settings > Secrets and variables > Actions
#
# SENTRY_ORG: Your Sentry organization slug (e.g., 'deepref')
# SENTRY_PROJECT_API: Backend project name (e.g., 'deepref-api')
# SENTRY_PROJECT_WEB: Frontend project name (e.g., 'deepref-web')
# SENTRY_AUTH_TOKEN: Authentication token from Sentry settings
#   Generate at: https://sentry.io/settings/account/api/auth-tokens/
#   Scopes needed: project:read, project:releases, org:read


# ----------------------------------------
# Optional Advanced Settings
# ----------------------------------------

# Sample rate for errors (1.0 = 100%, captures all errors)
# SENTRY_SAMPLE_RATE=1.0

# Maximum breadcrumbs to keep
# SENTRY_MAX_BREADCRUMBS=50

# Enable debug logging (development only)
# SENTRY_DEBUG=false

# Filter noisy transactions
# SENTRY_IGNORE_TRANSACTIONS=/health,/metrics


# ----------------------------------------
# Getting Started
# ----------------------------------------
#
# 1. Create a Sentry account at https://sentry.io
# 2. Create two projects:
#    - One for backend (Node.js)
#    - One for frontend (Angular)
# 3. Copy the DSN from each project
# 4. Fill in the SENTRY_DSN values above
# 5. Copy this file to .env
# 6. Restart your application
# 7. Generate a test error to verify integration:
#    curl http://localhost:3000/api/v1/test-sentry-error
#
# For detailed setup instructions, see:
# - /docs/SENTRY_QUICK_START.md
# - /docs/SENTRY_IMPLEMENTATION_SUMMARY.md
# - /docs/monitoring-runbook.md
